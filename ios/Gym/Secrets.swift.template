import Foundation

// Copy this file to `Secrets.swift` (Secrets.swift is gitignored).
//
// Production points at the live Vercel deployment. For local dev against
// `vercel dev --listen 3456`, change apiBase to http://localhost:3456.

enum Secrets {
    static let apiBase = URL(string: "https://dukhaansin.com")!
}
