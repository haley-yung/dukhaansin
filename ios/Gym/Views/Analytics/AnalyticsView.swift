import SwiftUI

struct AnalyticsView: View {
    var body: some View {
        PlaceholderView(title: "Analytics", subtitle: "Phase 7")
    }
}

#Preview {
    AnalyticsView()
        .preferredColorScheme(.dark)
}
