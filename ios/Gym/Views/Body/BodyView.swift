import SwiftUI

struct BodyView: View {
    var body: some View {
        PlaceholderView(title: "Body", subtitle: "Phase 8")
    }
}

#Preview {
    BodyView()
        .preferredColorScheme(.dark)
}
