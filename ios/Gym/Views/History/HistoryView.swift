import SwiftUI

struct HistoryView: View {
    var body: some View {
        PlaceholderView(title: "History", subtitle: "Phase 6")
    }
}

#Preview {
    HistoryView()
        .preferredColorScheme(.dark)
}
