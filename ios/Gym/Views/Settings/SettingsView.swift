import SwiftUI

struct SettingsView: View {
    var body: some View {
        PlaceholderView(title: "Settings", subtitle: "Phase 9")
    }
}

#Preview {
    SettingsView()
        .preferredColorScheme(.dark)
}
